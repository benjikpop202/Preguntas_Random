<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../styles/EstilosJuegos.css">
    <title>juego</title>
</head>
<body>
    <div class="contenedor">
     <main>
     <section>
     <% let m = Math.floor(Math.random() * preguntas.length) %>
     <% let random = preguntas[m] %>
      <div id="marco" "><%= random.pregunta %></div>  
       
      <button class="option" id="A" value="A"><%= random.opcion1 %></button>
    
      <button class="option" id="B" value="B"><%= random.opcion2 %></button>

      <button class="option" id="C" value="C"><%= random.opcion3 %></button>

     </section>

     <aside>

        <span id="contador">10</span>
        <a id="continuar" href="/juego">CONTINUAR</a>

     </aside>
     </main>
     <span id="<%= random.respuesta %>" class="hiddenSpan"></span>
    </div>
    <script src="../Functions/juego.js"></script>
</body>
</html>